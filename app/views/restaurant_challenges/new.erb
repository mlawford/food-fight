<%= form_for @restaurant_challenge do |f|%>
Restaurant 1: <%= f.collection_select :restaurant_id, Restaurant.all, :id, :name%> <br>
<!-- Restaurant 2: <%= f.collection_select :restaurant_id, Restaurant.all, :id, :name%> <br>
Restaurant 3: <%= f.collection_select :restaurant_id, Restaurant.all, :id, :name%> <br>
Restaurant 4: <%= f.collection_select :restaurant_id, Restaurant.all, :id, :name%> <br>
Restaurant 5: <%= f.collection_select :restaurant_id, Restaurant.all, :id, :name%> <br> -->

Challenge: <%= f.collection_select :challenge_id, Challenge.all, :id, :description%> <br>
<%= f.submit "Add Restaurants to Challenge" %>
<% end %><br>
<%= link_to "Back to Home", "/home" %>
